# エージェント作業ルール

## 使用言語

- ユーザーとのチャットは、ユーザーから明示的な指定がない限り日本語で行う。
- ADR、`docs/` 配下の文書、作業メモなど、エージェントが生成・更新する説明文書は
  日本語で記述する。
- 診断レポートなど、エージェントが追加するツールの利用者向けメッセージも日本語を
  基本とする。
- コード、識別子、ファイル名、コマンド、設定値、固有名詞、外部ツールの出力や引用は、
  正確性と既存の規約を優先し、必要に応じて英語のまま記述する。
- 既存文書を更新するときは、変更対象の周辺にある英語の説明も、無理のない範囲で
  日本語へ統一する。

## ADR

継続的な作業に影響する重要な技術判断は、`docs/README.md` の運用ルールに従って
`docs/ADR/` に記録する。

## ドキュメント運用（LLM wiki方式）

- 構造と基準の正本は `wiki/SCHEMA.md` とする。
- セッション開始時は `/context-load`、作業の区切り・push前は `/update-doc` を実行する。
- 新しい情報は必ず `raw/`（不変記録）を経由して `wiki/pages/` に蒸留する。

## 一時タスク

- 複数回の作業にまたがる未完了事項は `docs/tasks/` に記録する。
- タスク文書には現在の状態、完了済み項目、未完了項目、完了条件、次に行う作業、
  関連文書を記載する。
- 作業を進めたエージェントは、実装や検証結果と同じ変更内でタスク文書を更新する。
- 恒久的な技術判断はタスク文書だけに残さずADRへ記録し、タスク文書から参照する。
- タスクが完了したらステータスを `完了` に変更する。履歴として残す価値が低い
  一時メモは、必要な情報を恒久文書へ移してから削除してよい。

## ツール導入用cookbook

- 継続利用するツールを正式に導入するときは、原則として
  `cookbooks/<tool-name>/default.rb` に専用cookbookを作成する。
- 専用cookbookは、対象OSのroleまたは共通のbase roleから明示的に読み込む。
- 評価中または一時的に試しているだけのツールは、専用cookbookを追加してもよいが
  必須とはしない。
- 評価中のツールを継続利用すると決めた時点で、既存の一括インストール処理へ残さず
  専用cookbookへ移す。
- cookbookでは、可能な限り再実行しても不要なインストールを行わない条件を設け、
  対応していないOSでは何もせず成功させるのか、明示的に失敗させるのかを判断できる
  実装にする。

## CodexとClaudeの共通指示

- Codex向けの `AGENTS.md` とClaude向けの `CLAUDE.md` に同じ内容を重複して
  記述しない。
- どちらか一方をmasterとなる実体ファイルにし、もう一方はmasterへの相対
  シンボリックリンクにする。
- このリポジトリでは個人利用のCodexを基準とし、`AGENTS.md` をmaster、
  `CLAUDE.md` を `AGENTS.md` へのシンボリックリンクとして管理する。
- 共通ルールを変更するときはmasterだけを編集し、リンク先を直接編集しない。
- リンクが壊れていないことと、Gitがリンクとして記録していることを変更時に確認する。
- 別のリポジトリでClaudeをmasterにする場合は、`CLAUDE.md` を実体、
  `AGENTS.md` をそのシンボリックリンクとする。同一リポジトリ内でmasterを混在させない。
- `config/.claude/skills/` など、一方のエージェント固有の機能や設定は共通指示では
  ないため、この同期ルールの対象外とする。
