#!/bin/bash
# Homebrew does not allow sudo.

bin/setup

mitamae() {
  case "$(uname)" in
    "Darwin")  bin/mitamae local $@;;
    *) sudo -i sh -c "cd $(pwd) && bin/mitamae local $@" ;;
  esac
}

if [ "$1" = "" ]; then
  mitamae lib/recipe.rb
else
  mitamae $@
fi
